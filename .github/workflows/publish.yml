name: Publish to Maven Central

# Triggered on:
#   (a) GitHub release publication — uploads release bundles to Central Portal staging for
#       manual review (automaticRelease=false in build.gradle.kts → maintainer clicks "publish"
#       at https://central.sonatype.com/publishing).
#   (b) push to `main` — publishes the current `-SNAPSHOT` to Central Portal's snapshots repo
#       (no staging gate; snapshots are mutable and replace any prior copy of the same version).
#
# The same `publishToMavenCentral` Gradle task covers both: vanniktech inspects the project
# version and routes to the right endpoint (snapshots URL when version ends in -SNAPSHOT,
# staging-bundle API for releases).
#
# Required repository secrets:
#   SONATYPE_USERNAME         — Sonatype Central Portal user-token username
#   SONATYPE_PASSWORD         — Sonatype Central Portal user-token password
#   PGP_PRIVATE_KEY           — armored GPG private key (multi-line; full ASCII-armor block)
# Optional:
#   PGP_KEY_PASSWORD          — only needed if PGP_PRIVATE_KEY has a passphrase. Skip the
#                                secret entirely for unprotected CI keys; the env-var below
#                                falls back to an empty string in that case.

on:
  release:
    types: [published]
  # Snapshot publishes fire after CI completes — and only when BOTH Tier 1 (unit/TestKit)
  # and Tier 2 (MC runServer smoke) succeeded for the same commit. Tier 1 and Tier 2 still
  # run in parallel on push:main; this workflow listens for either to finish, then gates
  # itself on the other one's status before doing any work. The later-of-the-two firing
  # finds both green and proceeds; an earlier firing finds the second one still in progress
  # (or failed) and no-ops.
  workflow_run:
    workflows:
      - "CI — Tier 1 (unit + TestKit + manifest)"
      - "CI — Tier 2 (MC runServer smoke)"
    types: [completed]
    branches: [main]
  workflow_dispatch: # manual trigger for testing

concurrency:
  group: publish-${{ github.ref }}-${{ github.event.workflow_run.head_sha || github.sha }}
  cancel-in-progress: false # never cancel an in-flight publish

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      actions: read

    steps:
      - name: Gate on Tier 1 + Tier 2 success
        id: gate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          REPO: ${{ github.repository }}
          EVENT: ${{ github.event_name }}
          TRIGGERING_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
        run: |
          set -euo pipefail
          # release / workflow_dispatch paths bypass the gate; only workflow_run firings
          # check that both required workflows succeeded for this commit.
          if [ "$EVENT" != "workflow_run" ]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "$TRIGGERING_CONCLUSION" != "success" ]; then
            echo "Triggering workflow concluded with '$TRIGGERING_CONCLUSION'. Skipping publish."
            echo "publish=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          required=(
            "CI — Tier 1 (unit + TestKit + manifest)"
            "CI — Tier 2 (MC runServer smoke)"
          )
          for name in "${required[@]}"; do
            conclusion=$(gh run list \
              --repo "$REPO" \
              --workflow "$name" \
              --branch main \
              --commit "$HEAD_SHA" \
              --limit 1 \
              --json conclusion \
              --jq '.[0].conclusion // "none"')
            echo "  $name → $conclusion"
            if [ "$conclusion" != "success" ]; then
              echo "Required workflow '$name' not green at $HEAD_SHA (got: $conclusion). Skipping; the other workflow_run firing will retry the gate when it finishes."
              echo "publish=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          done
          echo "All required workflows green at $HEAD_SHA — proceeding."
          echo "publish=true" >> "$GITHUB_OUTPUT"

      - name: Checkout
        if: steps.gate.outputs.publish == 'true'
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.ref }}

      - name: Set up JDK 21 (Temurin)
        if: steps.gate.outputs.publish == 'true'
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Gradle home
        if: steps.gate.outputs.publish == 'true'
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-publish-${{ hashFiles('gradle/libs.versions.toml', '**/*.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-publish-
            gradle-ubuntu-latest-

      - name: Build everything (assert release-quality before upload)
        if: steps.gate.outputs.publish == 'true'
        run: ./gradlew build --no-daemon --stacktrace

      - name: Publish to Sonatype Central Portal
        if: steps.gate.outputs.publish == 'true'
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.SONATYPE_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.SONATYPE_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.PGP_PRIVATE_KEY }}
          # Optional — only needed for passphrase-protected keys. `|| ''` keeps the env var
          # set to an empty string when the secret is absent, which vanniktech accepts as
          # "no passphrase" without erroring on a missing variable.
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.PGP_KEY_PASSWORD || '' }}
        run: |
          set -euo pipefail
          # publishToMavenCentral runs the staging-bundle upload (vanniktech) for every
          # subproject that applies the plugin: :mcdp (the merged jar) and :gradle-plugin.
          ./gradlew publishToMavenCentral --no-daemon --stacktrace --no-configuration-cache
          echo "Bundles uploaded to Central Portal staging. Review and release at:"
          echo "  https://central.sonatype.com/publishing"
